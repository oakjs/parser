/* SPELL: added rule: `do something` */
function do_something() {
	if (1) {
		let it = 100
		/* PARSE ERROR: UNABLE TO PARSE: "+ card" */
	}
}
/* PARSE ERROR: UNABLE TO PARSE: "print it as lowercase and then do this and do that and do the other thing" */
return 1