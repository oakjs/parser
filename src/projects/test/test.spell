a card is a thing
a pile is a list of cards
c is a new card
p is a new pile

a pile "will accept anything" if
	print the it + the pile
	return true
p will accept anything

a pile "will accept (a card)" if
	print it + the pile + the card
	return true
if p will accept c then print "yES"